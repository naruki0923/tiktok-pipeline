# AIエージェント運用構想（最終形の設計メモ）

> このプロジェクトの最終ゴール。「AIによるSNS運用代行会社」を、複数のAIエージェントで無人運用する。
> ユーザー＝**社長**。ほぼ「OK / ダメ」の承認だけで会社が回る状態を目指す。
> ※ 各エージェントの「確認要件（ディレクター/PMが何を見るか）」は別途指定予定。ここは全体像の記録。

## 会社のメタファー
- **社長** … ユーザー本人。最終承認（投稿の可否・改善案の採否）だけを担当。
- **PM（副社長・責任者）** … 命令を受けて各エージェントに仕事を振る。各成果を確認し、次工程へ回す司令塔。
- **ディレクター（QA・確認者）** … 各エージェントの成果が「要件」を満たすか確認。NGなら担当agentへ差し戻し、OKならPMへ上げる。
- **各専門エージェント**（下の工程担当）。

## エージェント編成（現状の5ディレクトリに対応）
| エージェント | 担当 | 対応ディレクトリ | 現状 |
|---|---|---|---|
| リサーチ agent | 参考動画を探す（検索リサーチ） | 1_リサーチ | データ取得は半自動（TikTok検索→数値抽出）。best選定ロジック要 |
| コピーライター agent | 文字起こし→台本作成→整形 | 2_台本生成 | 文字起こし・整形は自動。**台本化は対話AI（Claude Code）が担当＝APIキー不要**。URLを渡せば台本まで書ける（プロンプト_台本生成/台本作成ルール準拠）。※ヘッドレス完全無人化する場合のみ将来Claude API化 |
| 動画編集 agent | 動画生成（縦型9:16） | 3_動画生成 | 自動（要VOICEVOX＋外付けSSD） |
| 投稿 agent | TikTok / YouTube へ投稿設定 | 4_投稿 | 実装済み（CDP接続で検知回避・半自動〜--auto） |
| データ分析 agent | 投稿後の指標分析→改善案立案 | 5_分析 | 部品実装済み（record.py/analyze.py）。**数値取得が手動＝自走の唯一のボトルネック** |

## 基本フロー（PMが司令塔、ディレクターが各所でQA）
```
① 何らかの方法で PM に「やれ」の命令が入る（トリガーは未確定。時間自動cronをまず目指す）
② PM → リサーチagent 起動 → リサーチ
      → ディレクターが要件確認 → NG:リサーチagentへ差し戻し / OK:PMへ
③ PM 確認OK → コピーライターagent → 文字起こし＋台本＋整形
      → ディレクターと確認ラリー → OK:PMへ → PM確認
④ PM 確認OK → 動画編集agent → 動画生成
      → ディレクター確認 → PMへ → PM確認
⑤ PM → 社長（ユーザー）へ「動画できました。投稿していいですか？」（LINE等、手段未定）
      → 社長が修正指示なら該当agentへ差し戻し（例:台本修正→コピーライター→ディレクター…のラリー）
      → 社長OKになるまで繰り返す
⑥ 社長OK → PM が 投稿agent 起動 → TikTok・YouTube へ投稿設定
⑦ 翌日以降 → データ分析agent が伸び/指標を分析（分析要件は別途指定）
      → PMと一緒に改善案を立案（例:「画像投稿が伸びてる／大手が画像に移行→画像に切替ては?」）
      → 社長に確認 → OKなら運用に反映（必要なら新agentを組成）
⑧ PDCA を人がほぼ入らず回す。社長は基本「OK/ダメ」だけ。
```

## 人間（社長）が介在するゲート
- **投稿前の最終承認**（⑤）
- **改善案の採否**（⑦）。改善案の提案頻度は毎日 or 週1（未定）
- それ以外は自動で「止める/差し戻す」判定を各agent・ディレクターが返す（目視前提にしない）

## トリガー（PMへの号令の入口）… 未確定、候補
- **cronは使わない**（本人意向）。時間で自動、は一旦なし。
- Google Apps Script（手動 or 何かのイベントで号令）／自前の通知
- LINE連携（LINEに送られたら起動、LINEで承認、等）
- ※ どれも「入口」。実行本体はローカル（動画生成がVOICEVOX＋SSD必須のため完全クラウド不可）。
- ※ 号令方法は未決。「何かしらの命令でリサーチ開始→動画まで」を自動化したい、という要件のみ確定。

## 現実的な作り方（コンパクトに段階導入）
1. **オーケストレーター `run.py`** … ①〜⑤の工程スクリプトを1本に束ねる（号令＝手動コマンド等でこれを起動。トリガー手段は未定）。
2. 足りない部品を順に埋める: `research.py`（best自動選定）／投稿agent／分析agent。
3. 台本化の自動/手動は未確定（当面は人 or 対話AIが担当でも可）。将来Claude API化で無人化も選べる。
4. エージェント化は、疎結合な各工程スクリプトを PM/ディレクターの制御層でラップする形で段階移行。

## 実装状況（2026-07-16）
この構想の骨格（`統括/run.py` オーケストレーター＋ `.claude/agents/*.md` の8体）を一度実装したが、
**同日に社長判断で削除した**。各工程は従来どおり手動スクリプト（2〜5の各scripts）で回す運用に戻す。
この構想ドキュメント自体は将来の再エージェント化のために残す（＝設計メモ）。
- 実運用で1本を通した検証（本番_017: リサーチ→台本→リーガル→動画→投稿ゲート）は完了しており、
  「各工程スクリプト＋人間の判断」で回ること自体は確認済み。エージェントはその上の自動制御層だった。
- **3Dオフィスの可視化だけは残置**: `統括/ダッシュボード/`（`server.py`＋`dashboard.html`）。
  会社メタファーでパイプラインを見せるデモ。
- 当面のフォーカスは投稿本数を稼いでデータを溜めること（メモリ roadmap-decisions-2026-07）。

## データ分析agentのPDCA自走設計（5_分析）
最終形は「投稿後、人が触らずに指標を取り→評価し→次のネタ/改善案を 1_リサーチ に流し込む」ループ。
PDCAの各段と現状:

| 段 | 内容 | 担当工程 | 現状 |
|---|---|---|---|
| **Plan** | ネタ・型・フックを決める | 1_リサーチ→2_台本 | 半自動（best選定ロジック未） |
| **Do** | 動画生成→投稿 | 3→4 | 実装済み |
| **Check** | 指標取得→勝ち/負け判定 | 5_分析 | 判定は自動（analyze.py）。**取得が手動** |
| **Act** | 改善案を次のPlanへ反映 | 5→1 | レポートは自動。**流し込みが手動** |

**自走に必要な残り3部品**:
1. **metrics_fetch.py（Checkの無人化）** … 実装済み（一部）。TikTok Studioのコンテンツ一覧を
   browser_ctx（CDP後接続で検知回避）で読み、post_logのキャプションで動画を突合して
   **再生数・いいね・コメント・尺・投稿日**を metrics.csv に自動追記。実データ（本番_015=88再生）で検証済み。
   ※投稿24h後の固定時刻に実行する運用。
   ⚠**未取得＝平均視聴秒/継続率1秒/おすすめ%/視聴者属性**（＝最重要の視聴維持率の材料）。
   動画別詳細分析がURL直叩き不可（分析ホームに戻される）。次段: 一覧の行クリック→詳細パネルからの抽出、
   またはモバイルアプリ側の自動化を検討。当面この4項目は record.py で手動追記。
2. **feedback / 改善反映（大きさで2段階）** … ★重要な設計方針。
   Actは改善の大きさで分ける:
   - **① 小さい戦術的改善＝自動でやる（聞かない）**: 今の型/形式の中の調整。
     例: **フック（冒頭）の変更**・テンポ・テロップ・尺・CTA位置・BGM等の演出。勝手に次の台本/動画へ反映。
   - **② 大きい戦略的ピボット＝聞く（人間承認ゲート＝構想⑦）**: 形式/方向性を変える提案は自動反映しない。
     「分析＋リサーチのトレンド」を組み合わせた改善提案を社長に上げ OK/却下を仰ぐ。
     例:「あちこちで画像投稿がバズっている／競合も画像に移行 → 画像形式に切り替えますか？」
     承認された提案だけが次サイクルに入る（自動では入れない）。
   - 線引き＝「型/形式の中の調整」か「型/形式そのものの変更」か。
   - 材料: 5_分析（勝ち/負け・視聴者到達＝analyze.py）＋ 1_リサーチ（外部トレンド検出。**未実装＝②提案の片翼が未完**）。
3. **制御ループ（PM層）** … 「投稿→翌日fetch→analyze→feedback→次のPlan起動」を束ねる。
   トリガーはcron不使用方針のため、投稿agentが翌日の自分自身の後続タスクを予約する形（イベント駆動）を想定。

**人間ゲートは1つに集約**: ⑦の「改善案の採否」。fetch/analyze/feedbackは無人、
社長は分析レポート＋改善案に「反映OK/却下」だけ返す。判定しきい値（維持率/継続率/おすすめ%）は
analyze.py 冒頭の定数で、運用で育てながらチューニング。

## 関連ルール・前提
- 参考動画は **30日間は再利用禁止**（`1_リサーチ/採用履歴.tsv` で自動チェック）。
- 候補が複数なら **その時点で最良の1本**を採用。
- 誇大・断定表現を避ける（リーガルチェックの基準は「断定回避」でなく「誤認させない」）。
- 動画生成の前提: VOICEVOX.app 起動＋外付けSSD「Extreme SSD」接続。
