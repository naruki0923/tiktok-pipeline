# TikTok動画 自動生成・自動投稿プロジェクト

## プロジェクト概要
TikTokアフィリエイト用の動画を自動で作成し、自動で投稿するシステムを構築するプロジェクト。
テーマは「退職給付金」（退職を検討している人向けに、国の制度を使って給付金を受け取る方法を案内する内容）。

## 対象チャンネル
- チャンネル名: `.env` の TIKTOK_ACCOUNT に書く（コードとドキュメントには書かない）
- URL: https://www.tiktok.com/@<TIKTOK_ACCOUNT>
- 現状のプロフィール文言: 「退職を考えるなら必見✨ 国の制度で給付金をGETしませんか？ 3000人以上サポート実績あり！」
- 導線: プロフィールのLINE公式アカウント（無料説明会への登録リンク）へ誘導する構成
- ジャンル: 退職代行/退職給付金（失業保険・re-employment支援金など公的制度）に関する情報発信

## 目的
- 「退職給付金」に関する訴求内容（制度紹介・悩み共感・実績訴求等）をもとに、TikTok向け動画を自動生成する
- 生成した動画を上記チャンネルへ自動投稿する
- 動画からLINE公式アカウントの無料説明会への登録導線（プロフィールリンク経由）を意識した構成にする
- 一連の流れ（企画→素材収集→動画生成→投稿）を可能な限り自動化・省人化する

## 想定パイプライン（現時点の構想）
1. **ネタ選定**: 退職給付金・失業保険・退職代行まわりの訴求パターン（悩み共感/制度解説/実績・体験談/Q&A等）のリストアップ
2. **台本・構成生成**: テーマ（退職給付金）に沿ったTikTok向け台本・テロップ・フック文言を生成（最後にLINE誘導のCTAを入れる）
3. **素材収集/生成**: 画像・動画クリップ・音声（ナレーション/BGM）の準備
4. **動画合成**: 台本・素材を組み合わせて動画ファイルを生成（字幕焼き込み等含む）
5. **投稿**: TikTokへのアップロード（API/自動化ツール経由）
6. **記録・分析**: 投稿履歴、再生数、クリック数などのログ管理

## ディレクトリ構成
工程ごとに5つのディレクトリに分割している。データは 1→2→3→4→5 の順に流れ、5の分析結果を1へフィードバックするループ構造。

- `1_リサーチ/`: ネタ選定・競合分析・制度ファクトの調査 → ネタ案を出力（**自動化済み**）
- `2_台本生成/`: リサーチ結果から台本・テロップ・キャプションを生成（**自動化済み**）
- `3_動画生成/`: 台本から動画ファイル（縦型9:16）を生成（**実装済み・自動**）
- `4_投稿/`: 投稿前チェック → TikTok へ投稿 → 投稿ログを記録
- `5_分析/`: 投稿の指標・成果を分析し、1_リサーチ へフィードバック

各ディレクトリの詳しい役割・入出力は、それぞれの `README.md` を参照。

## 現状の進捗
- **3_動画生成 は自動化済み**。台本テキストを渡すだけで完成動画（TikTok用/YouTube用の2本）が出る:
  ```bash
  cd 3_動画生成/scripts && ./.venv/bin/python make_video.py ../音声/本番_013.txt
  ```
  中身は VOICEVOX（青山龍星・1.2倍速）で音声＋各行タイミング生成 → ffmpeg で
  背景（4K散歩動画）＋縦書きテロップ＋SE＋いいね/LINE誘導オーバーレイを合成。
  詳細は [3_動画生成/scripts/README.md](3_動画生成/scripts/README.md)。
  - 前提: VOICEVOX.app 起動中／外付けSSD「Extreme SSD」接続。
- **2_台本生成 の①②③は自動化済み**（scripts に venv 1つ。python3.13）:
  ```bash
  cd 2_台本生成/scripts
  ./.venv/bin/python transcribe.py "https://vt.tiktok.com/XXXX/" -o ../文字起こし/ref_001.txt  # ①② DL+文字起こし
  # ③ 文字起こし → AI台本化（プロンプト_台本生成.md）→
  ./.venv/bin/python format_script.py 台本.txt -o ../../3_動画生成/音声/本番_014.txt          # ③ 整形+検査
  ```
  - **transcribe.py**: yt-dlp＋faster-whisper(large-v3)。文字起こしは下書き（数字・同音異義語の誤りあり）。
  - **format_script.py**: 句点→空行・読点→行区切り・句読点除去・30字改行（fugashi、telop_render と共有）＋CTA/いいね/行長を検査。
  - **AIが担当**: 台本化（完コピ・固定文言・です/ます統一・誤り修正）。台本の型は [2_台本生成/台本作成ルール.md](2_台本生成/台本作成ルール.md)。
  - 参考動画URLは台本シートG1。過去台本11本は Google Drive。
- **1_リサーチ・台本化も自動化済み（2026-08-03）**。対話なしで参考動画の選定〜台本まで通る:
  ```bash
  cd 1_リサーチ/scripts && ../../4_投稿/scripts/.venv/bin/python auto_research.py   # 候補JSON
  cd 2_台本生成/scripts && ./.venv/bin/python write_script.py --ref ../文字起こし/ref_027.txt
  ```
  - **auto_research.py**: `/search/video?q=` のグリッドから動画URLを集め、**動画ページの埋め込みJSON
    （`__UNIVERSAL_DATA_FOR_REHYDRATION__`）から再生数・尺・投稿日・いいね・保存・フォロワー**を取り、
    **黄金式(フォロワー×100＜再生)・実尺45〜180秒・30日再利用禁止**で絞って候補JSONを出す。
    手順の根拠は memory `research-tiktok-scrape-method`。
    - **数値取得はブラウザが本線・yt-dlpは保険**（2026-08-11に入れ替え）。TikTokは日によって
      **yt-dlpのリクエストにだけ**botチャレンジを返し、全URLが `Unexpected response from
      webpage request` で落ちる。旧構成（実数取得がyt-dlp）だとその日は**必ず候補ゼロ**になり、
      1本3回リトライするので30分粘った末にゼロで返っていた。同じ時刻に実物のChromeでは
      動画ページが普通に開ける。同じ理由で **`transcribe.py` の動画DLも
      `1_リサーチ/scripts/tiktok_dl.py`（ブラウザ経由）へフォールバック**する。
    - **Chromeプロファイルは投稿用と分離**: リサーチは `1_リサーチ/scripts/.chrome-research`・
      ポート**9223**・**未ログイン**。投稿は `.chrome-profile`・9222・ログイン済み。
      業務アカウントでスクレイピングして垢を巻き込まないため（2026-08-03 社長判断）。
    - 開きっぱなしのChromeは**HTTPに答えるのにCDPのWebSocketだけ固まる**ことがあり、
      これで18:00の無人実行が落ちた。`browser_ctx.connect_healthy()` が繋がらなければ
      kill して起動し直す（生存判定はポートではなく `pgrep`）。
  - **write_script.py**: 文字起こし＋`台本作成ルール.md`全文を1つのプロンプトに載せ、
    **①季節性判定 ②角度の重複判定 ③台本 ④キャプション を1回のLLM呼び出しで**返させる。
    通過後 `format_script.py` に流し、`--title-lines`（フック1文目の行数・上限3）も算出する。
    尺が75秒を超えた時だけ、項目を減らす短いプロンプトでもう1回だけ叩く。
  - **LLM経路は `2_台本生成/scripts/llm.py`**。既定は**契約済みの `claude` CLI をヘッドレス実行（追加課金なし）**。
    `ANTHROPIC_API_KEY` を置いた時だけ従量課金APIに切り替わる。**呼び出しは動画1本につき1〜2回だけ**。
- **4_投稿 を実装・実地検証済み**（2026-07-15に初回の予約投稿を通した）。1コマンドで
  **アップロード→キャプション→BGM追加→音量-20dB→翌朝に予約** まで自動:
  ```bash
  cd 4_投稿/scripts
  ./.venv/bin/python login.py                                    # 初回のみ手動ログイン→.chrome-profile保存
  ./.venv/bin/python post_tiktok.py ../../3_動画生成/output/本番_015_TikTok.mp4 --caption-file ../投稿予定/本番_015.txt
  ```
  - **検知回避が肝**: TikTokはPlaywright起動Chromeを検知しログインをブロックする。そこで
    `browser_ctx.py` は自動化フラグなしでChromeをsubprocess起動し **connect_over_cdp で後接続**。
    動画は50MB制限を避け **CDPの DOM.setFileInputFiles** で指定。セッションは `.chrome-profile/`（gitignore）。
  - **editor_ops.py**: 編集画面のBGM追加（お気に入りの「Ghibli-like piano solo ballad」）・音量-20dB・保存・予約（時刻は `--schedule-time`。Discordbot経由なら翌朝8:30）。
    **曲名は英訳名と原題の両方で探す**（`BGM_ALIASES`）。TikTokがどちらで表示するかは向こうの都合で変わり、
    2026-08-20に原題表示へ変わった時は英語名しか見ておらず **15本連続でBGM無しのまま公開**された（2026-09-05に発覚）。
    足せなかった時は `post_log.csv` に `bgm_failed` を残す。Discordへの⚠️が出なかったのは
    `runner._run` が末尾1800字だけ返して序盤の警告を捨てていたためで、こちらも重要行を残すよう直した。
  - **既定は半自動**（設定まで自動、最後の【投稿予約する】は人間）。運用が育ったら `--auto`。運用ペースは1日1本。
  - **precheck.py**: 投稿手段に依らない投稿前チェック（縦型9:16/尺/サイズ/キャプション長/CTA有無/誇大表現）。
  - 規約はグレー（ブラウザ自動化）。将来のクリーンな道は公式Content Posting API。
  - キャプションは `4_投稿/投稿予定/<動画名>.txt`。ログは `4_投稿/ログ/post_log.csv`。詳細は [4_投稿/scripts/README.md](4_投稿/scripts/README.md)。
- **YouTube Shorts 投稿を実装（2026-07-21）**。TikTokと違い**公式 YouTube Data API v3**（規約クリーン・UI変更で壊れない・予約公開が確実）。`*_YouTube.mp4`（縦9:16）を上げる:
  ```bash
  cd 4_投稿/scripts
  ./.venv/bin/python youtube_auth.py                             # 初回のみ OAuth（要 client_secret.json）
  ./.venv/bin/python youtube_upload.py ../../3_動画生成/output/本番_020_YouTube.mp4 --caption-file ../投稿予定/本番_020.txt
  ```
  - 1コマンドで **precheck → アップロード → タイトル/説明/タグ設定 → 翌日6:30(JST)に予約公開**（`publishAt`）。precheck・キャプションファイルはTikTokと共用。
  - **タイトルは自動生成**（先頭の【…】か冒頭文＋`#Shorts`）。タグはキャプションの`#…`から抽出。`--title`/`--publish-now`/`--privacy`等で上書き可。
  - **社長の初回準備が必要**: Google Cloudで OAuthクライアント(デスクトップ)を作り `4_投稿/scripts/client_secret.json` に配置 → `youtube_auth.py`。以後トークン自動リフレッシュ。手順はREADME。
  - `youtube_auth.py`/`youtube_upload.py`。ログは `4_投稿/ログ/youtube_log.csv`。機密(`client_secret.json`/`youtube_token.json`)はgitignore。
- **5_分析→1_リサーチ フィードバック** の仕組みを実装（2026-07-16）。`5_分析/scripts/feedback_context.py`（材料収集＋データ充足度🅐〜🅓の自動判定）＋`5_分析/プロンプト_フィードバック.md`（AIが `1_リサーチ/ネタ提案_YYYYMMDD.md` を下書き）。採用は社長。**2026-09-07から毎週日曜22:00に自動発火**（下の週次レビュー）。
- **AIエージェント層（`.claude/agents/*.md`・`統括/run.py`）は2026-07-16に一度実装したが、同日削除した**（社長判断）。
  各工程は従来どおり手動スクリプト（2〜5の各scripts）で回す。将来のエージェント化構想は [AIエージェント構想.md](AIエージェント構想.md) に残す。
- **3Dオフィスの可視化**は残置: `統括/ダッシュボード/`（`python3 統括/ダッシュボード/server.py` → http://localhost:8756）。会社メタファーでパイプラインを可視化するデモ。
- **Discord Bot に移行（2026-08-03・現行のスマホ操作はこちら）**。`統括/Discordbot/run.sh` を常時起動Macで動かす。
  **毎日06:00にリサーチ→台本→動画生成まで全自動**で走り、Discordにプレビューが届く。
  **毎週日曜22:00には週次レビュー**（分析→検索語・台本方針の更新）が走る（時刻は `.env` の `AUTO_TIME` が既定で、`state.json` の `auto.time` があればそちらが優先。2026-09-05に両方06:00へ揃えた）。**「投稿」と送った時だけ**投稿:
  ```bash
  cd 統括/Discordbot && cp .env.example .env   # DISCORD_BOT_TOKEN / DISCORD_CHANNEL_ID を記入（初回のみ）
  ./.venv/bin/python selftest.py                # 準備できてない項目を洗い出す
  ./run.sh                                      # 起動（開けっぱなしにしておく）
  ```
  - **トンネル(cloudflared)もwebhook登録も不要**。Botが自分からDiscordに常時接続を張るのでLINE版の弱点が消えた。
  - **動画1本＝スレッド1つ**（2026-08-05）。チャンネルに残るのは見出し1行（完成時に
    `└ ✅ 本番_037 タイトル` が書き足される）だけで、進捗・プレビュー・投稿結果はスレッド内。
    スレッドの中でそのまま `投稿`/`直して` と話しかけられる（許可判定は親チャンネルで行う）。
    紐づけは `state.json` の `videos.<name>.thread_id`。権限が無ければ従来どおり直書きに退避。
  - コマンド: `作って`（リサーチから全自動）/ `035 作って`（台本指定）/ TikTokのURLを貼る /
    **`投稿`**（両方に予約。**TikTok 翌朝8:30／YouTube 翌朝6:30**＝`runner.SLOTS`）/ **`おまかせ投稿`** / `投稿 035` / `夜 投稿`（18:30）/
    `毎日20時` / `自動オフ` / **`分析`（週次レビューを今すぐ）** / `週次オフ` / `ヘルプ`。
    プレビュー下のボタンでも可（青=手押し・赤=おまかせ）。
    **ボタンは `discord.ui.DynamicItem` なのでBot再起動後も有効**（2026-08-05に修正。
    以前は再起動で全部死んで「時間内に応答しませんでした」になった）。
  - **TikTokの【投稿予約する】は既定で社長が手押し**（`post_tiktok.py` を `--auto` 無しで呼ぶ）。
    Macの前に居られない時だけ `おまかせ投稿` で確定まで自動。YouTubeは公式APIなので常に自動。
    半自動は10分待っても押されないと終了コード0で返るため、`runner.post_tiktok` は
    ログの「確定しました/確定を検知」を見て **confirmed / awaiting / failed** の3状態で返す
    （押していないのに「✅予約完了」と誤報告しないため）。
  - **投稿は必ず社長の合図が要る**。自動実行がやるのは「作って見せる」まで。何も言わなければ何も出ない。
  - **完成＝「あとは投稿だけ」の時はメンションで呼ぶ**（2026-08-06）。プレビューの先頭に `@社長` が付き
    スマホに通知が飛ぶ。鳴るのは完成時だけで `見せて` では鳴らさない。相手は `.env` の
    `DISCORD_MENTION_USER_ID`（無ければ `DISCORD_ALLOWED_USER_ID`）、どちらも空なら
    起動時にチャンネルの直近の発言者を覚えて `state.json` の `owner_id` に残す（設定不要）。
  - LINE版のバグを修正済み: `make_video.py --title-lines` を必ず渡す／TikTokキャプションは
    `投稿予定/本番_NNN_TikTok.txt`（短い形式）を優先。生成と投稿は `_busy` ロックで同時実行しない。
  - **工程エラー時はCodex CLIが自動で原因調査・修正・確認する**（2026-08-15追加）。
    `codex login status` がChatGPTログインを示す時だけ動き、APIキーは子プロセスへ渡さない。
    投稿・アップロードは行わず、結果を同じDiscordスレッドへ返す。`.env` の
    `AUTO_CODEX_RESCUE=0` で停止できる。
  - **生成がコケたら社長を待たずに自動で作り直す**（2026-08-17 社長判断）。
    Codexが直す →Botが自分の `config`/`runner`/`pipeline` を **importlib.reload** で
    読み直す → もう一度作る、を `AUTO_RETRY_MAX`（既定2）回まで。工程スクリプトは
    subprocessなので毎回新しいものが動くが、Botが抱えるモジュールは起動時のままで、
    読み直さないと「直したのに同じエラー」になる。`bot.py` 自身が直された時だけ再起動が要る
    （その旨をDiscordに出す）。コードが直らなかった時（TikTok側の一時制限など）は
    `AUTO_RETRY_WAIT`（既定300秒）空けて再試行。**投稿は対象外＝失敗しても勝手にやり直さない**。
  - 社長の初回準備: Discord Developer Portalで Bot作成→**MESSAGE CONTENT INTENT を ON**→サーバーに招待→
    チャンネルIDをコピー→`.env`記入。手順は [統括/Discordbot/README.md](統括/Discordbot/README.md)。機密(`.env`)はgitignore。
- **LINE Bot（2026-07-21・Discord Botに置き換え済み。参考として残置）**。`統括/LINEbot/run.sh`:
  ```bash
  cd 統括/LINEbot && cp .env.example .env   # LINEのシークレット/トークンを記入（初回のみ）
  ./run.sh                                   # トンネル→webhook自動登録→待受
  ```
  「021 作って」→ 台本 `本番_021.txt` から生成 → **プレビュー動画＋投稿ボタン**がLINEに届く → ボタン（＝社長の承認ゲート）で TikTok/YouTube に予約投稿。既存スクリプトは無改変でsubprocess呼び出し。
  - **自動配信（時間トリガー）も実装**。LINEで「毎日20時」と打つとその時刻に次の台本を自動生成し「🤖(自動) 本番_014 作るよ」＋プレビュー＋確認ボタンが勝手に届く。「自動オフ」で停止・「自動」で状態表示。**時刻設定はLINEチャットで完結（Mac/cron不要）**、時計は `scheduler.py`（schedule.jsonに永続化）。**投稿は常にボタンを押した時だけ＝押さなければ投稿されない**（承認ゲート維持）。
  - **トンネルは cloudflared クイックトンネル**（アカウント/ポート開放不要）。公開URLは起動ごとに変わるが **LINEのwebhook先を起動時に自動登録**するのでコンソールへの貼り直し不要。
  - **v1スコープ外**: 台本づくり（AI創作）は当面Claude側。動画化には事前に台本txtが必要。参考URL→台本のLINE化は将来 Claude API 統合で（v2）。
  - **社長の初回準備**: LINE Developersで Messaging APIチャネル作成→シークレット/トークン取得→Bot友だち追加→`.env`記入。手順は [統括/LINEbot/README.md](統括/LINEbot/README.md)。機密(`.env`)はgitignore。
- **伸びる型に寄せた（2026-09-04・実測フィードバック）**。042〜066の実数23本＋アカウントインサイトから、
  当たり筋は「年齢トリガー（60歳・65歳）」「期限」「取られるお金を減らす」、沈むのは「手続き・ステップ解説」
  「金額の大きさだけの訴求」と判明（上位2本で全再生の80%）。視聴者は**55歳以上が69.4%**。
  詳細は [5_分析/レポート/分析_20260904.md](5_分析/レポート/分析_20260904.md)。反映した箇所:
  - **`write_script.py` の重複判定を実績ベースに作り直した**。以前は `採用履歴.tsv` のメモ欄を
    「既に扱った角度」として渡していたが、自動運用では採用行のメモが空で、埋まるのは
    `pipeline.mark_rejected` が書く【不採用】理由だけ。**一度も作っていない角度を"扱い済み"として渡していた**ため、
    当たり筋（国保・住民税の減免＝032で243,000再生）が8/17〜9/3で9本連続の不採用になっていた。
    いまは `5_分析/実績.tsv` を読み、**1万再生以上＝【勝ち筋】は重複を理由に落とさない**（むしろ再訪）、
    1万未満＝【沈んだ角度】は不採用、公開7日未満＝【判定中】。
  - **`5_分析/scripts/update_results.py` を追加**。角度×再生数を `5_分析/実績.tsv` に書き出す。**週1回まわす**:
    ```bash
    cd 5_分析/scripts && ../../4_投稿/scripts/.venv/bin/python update_results.py
    ```
  - `台本作成ルール.md` に「★どの型で書くか — 伸びる型」を追加（優先する型/書かない型、
    **1行目の数字は年齢・期限・日数を選ぶ。金額の大きさは効かない**）。
  - `auto_research.py` の検索語を年齢トリガー・取られるお金へ寄せ、手続き系の語を外した。
- **週次レビューを自動化（2026-09-07）**。**毎週日曜22:00**に Discord Bot が
  `5_分析/scripts/weekly_review.py` を回し、**分析の結果をそのまま次の生成に反映**して結果をDiscordへ送る:
  ```bash
  cd 5_分析/scripts && python3 weekly_review.py            # 手動でも回せる（Discordで `分析`）
  python3 weekly_review.py --no-fetch --dry-run            # Chrome不要・書き換えなしの下見
  ```
  ① `update_results.py` で実績.tsv を最新に → ② `feedback_context.py` の材料束＋角度×再生数 →
  ③ **LLMを1回だけ**呼ぶ（`5_分析/プロンプト_週次反映.md`）→ ④ 反映 → ⑤ ネタ提案の下書き。
  - **機械が書き換えるのは2ファイルだけ**: `1_リサーチ/検索語.json`（`auto_research.py` の検索語）と
    `2_台本生成/重点方針.md`（`write_script.py` が台本プロンプトに丸ごと差し込む）。
    **`台本作成ルール.md` やコードには触らない**。人が書く場所と機械が書く場所を分けてある。
    2026-09-04 に手でやった「当たり筋へ検索語を寄せる」作業が、これで毎週勝手に回る。
  - **検索語は1語でも形が変なら丸ごと据え置く**。ここが壊れると翌朝から1本も作れなくなるため。
    JSONが無い・壊れている時は `DEFAULT_KEYWORDS` に戻る。
  - 上書き前の値は `5_分析/レポート/週次_YYYYMMDD.md` に丸ごと残る（git でも戻せる）。
  - **ネタ提案は今までどおり下書き**（`1_リサーチ/ネタ提案_YYYYMMDD.md`）。採用/却下は社長。
    **投稿には一切触らない**ので、走っても勝手に何かが世に出ることはない。
  - Discordのコマンド: `分析`（今すぐ）/ `毎週日曜22時`（曜日・時刻変更）/ `週次オフ` / `週次`。
    実測の取り直しで投稿用Chromeを開くので、生成・投稿とは同じロックで排他する。
- **方針（2026-07-16）**: 投稿系の完全無人化(--auto)・日次スケジューラは作らない（人間が起動・確定）。当面は投稿本数を稼いでデータを溜める。

## 注意事項・ルール
- TikTokの利用規約・広告表記（PR表記等）・各アフィリエイトプログラムの規約を遵守すること
- 退職給付金・失業保険等は国の公的制度に関わる内容のため、誇大広告・断定的表現（「必ずもらえる」等）や誤解を招く表現を避け、事実に基づいた説明にする
- 自動投稿は無人実行されるため、投稿前チェック（内容・著作権・規約違反・薬機法/景表法的な誇大表現の有無）の仕組みを組み込むこと
- APIキーやログイン情報、LINE連携情報などの機密情報は `.env` 等に分離し、リポジトリにコミットしない
- 生成物（動画・台本）の著作権・引用ルールに注意する

## 未決定事項（要検討）
- ~~動画生成に使用するツール/API~~ → **決定済み: VOICEVOX（ローカルAPI）＋ ffmpeg / Pillow**（`3_動画生成/scripts/`）
- TikTok投稿の自動化手段（公式API有無、サードパーティツール、ブラウザ自動化等）
- 投稿スケジュールの管理方法（cron、スケジューラ等）
- アフィリエイトの報酬体系（LINE登録課金/成果報酬型か、ASP経由か等）

## 開発時の方針
- まず最小構成（1商品→1本の動画→手動確認→投稿）で通しの動作を作ってから自動化範囲を広げる
- 各工程（生成/投稿）は疎結合にし、個別にテスト・差し替えできるようにする
